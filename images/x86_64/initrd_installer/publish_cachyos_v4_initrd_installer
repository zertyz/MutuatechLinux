#! /bin/bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"

ARTIFACTS="${ARTIFACTS:-artifacts/cachyos-x86_64_v4}" \
TARGET_ARCH="${TARGET_ARCH:-x86_64_v4}" \
KERNEL_BASENAME="${KERNEL_BASENAME:-vmlinuz-linux-cachyos}" \
INITRAMFS_BASENAME="${INITRAMFS_BASENAME:-initramfs-linux-cachyos.img}" \
XZ_LEVEL="${XZ_LEVEL:-9}" \
../../common/publish_initrd_artifacts
