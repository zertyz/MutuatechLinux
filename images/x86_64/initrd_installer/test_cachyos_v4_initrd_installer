#! /bin/bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"

TEST_IMG="${TEST_IMG:-${SCRIPT_DIR}/../artifacts/test-initrd-installer-cachyos-x86_64_v4.img}"

SRC_IMG="${SRC_IMG:-${SCRIPT_DIR}/../artifacts/mutuatechlinux-general-cachyos-x86_64_v4.img}" \
TEST_IMG="$TEST_IMG" \
ARTIFACTS_DIR="${ARTIFACTS_DIR:-${SCRIPT_DIR}/artifacts/cachyos-x86_64_v4}" \
KERNEL_BASENAME="${KERNEL_BASENAME:-vmlinuz-linux-cachyos}" \
INITRAMFS_BASENAME="${INITRAMFS_BASENAME:-initramfs-linux-cachyos.img}" \
WIPE_ROOT="${WIPE_ROOT:-1}" \
../../common/prepare_initrd_installer_test_image

IMG="$TEST_IMG" "${SCRIPT_DIR}/../test_cachyos_v4_image"
