KERNEL_PKG="${KERNEL_PKG:-linux}"
INITRAMFS_PREFIX="${INITRAMFS_PREFIX:-linux}"

# Prefer a live mirror before the shared kernel reinstall step.
sed -i 's|^#\(.*\.br/.*\)|\1|' /etc/pacman.d/mirrorlist
